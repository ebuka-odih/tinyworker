# Backend API base URL (without trailing slash).
VITE_API_BASE_URL="http://localhost:4000/api"

# Optional override for TinyFish long-running run endpoint.
# Useful when deployed behind a proxy that times out upstream calls.
VITE_TINYFISH_BACKEND_URL="https://tinyworker-production.up.railway.app"
